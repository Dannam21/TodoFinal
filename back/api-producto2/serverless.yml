org: dannamendez
service: api-producto

provider:
  name: aws
  runtime: python3.12
  memorySize: 1024
  timeout: 30
  iam:
    role: arn:aws:iam::385301667309:role/LabRole
  environment:
    PRODUCTO_TABLE: ${sls:stage}-proyecto_productos

functions:
  createProducto:
    handler: create_producto.lambda_handler
    events:
      - http:
          path: producto/create
          method: post
    environment:
      TABLE_NAME: ${self:provider.environment.PRODUCTO_TABLE}

  getProducto:
    handler: get_producto.lambda_handler
    events:
      - http:
          path: producto/get
          method: get
    environment:
      TABLE_NAME: ${self:provider.environment.PRODUCTO_TABLE}

  deleteProducto:
    handler: delete_producto.lambda_handler
    events:
      - http:
          path: producto/delete
          method: delete
    environment:
      TABLE_NAME: ${self:provider.environment.PRODUCTO_TABLE}

  listProductos:
    handler: producto/handler_list_productos.lambda_handler
    events:
      - http:
          path: producto/list
          method: get
    environment:
      TABLE_NAME: ${self:provider.environment.PRODUCTO_TABLE}

  updateProducto:
    handler: producto/handler_update_producto.lambda_handler
    events:
      - http:
          path: producto/update
          method: patch
    environment:
      TABLE_NAME: ${self:provider.environment.PRODUCTO_TABLE}

resources:
  Resources:
    ProductosDynamoDbTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.PRODUCTO_TABLE}
        AttributeDefinitions:
          - AttributeName: producto_id
            AttributeType: S
          - AttributeName: tenant_id
            AttributeType: S
          - AttributeName: stock
            AttributeType: N
          - AttributeName: nombre
            AttributeType: S  # Agregamos el nombre al esquema de atributos
          - AttributeName: categoria_nombre
            AttributeType: S  # Asegúrate de que esta propiedad esté incluida
        KeySchema:
          - AttributeName: producto_id
            KeyType: HASH
          - AttributeName: stock
            KeyType: RANGE
        LocalSecondaryIndexes:
          - IndexName: LSI_Stock
            KeySchema:
              - AttributeName: producto_id
                KeyType: HASH
              - AttributeName: stock
                KeyType: RANGE
            Projection:
              ProjectionType: ALL
        GlobalSecondaryIndexes:
          - IndexName: GSI_TenantID_CategoriaNombre
            KeySchema:
              - AttributeName: tenant_id
                KeyType: HASH
              - AttributeName: categoria_nombre
                KeyType: RANGE
            Projection:
              ProjectionType: ALL
            ProvisionedThroughput:
              ReadCapacityUnits: 5
              WriteCapacityUnits: 5
          - IndexName: GSI_productoID_nombre
            KeySchema:
              - AttributeName: producto_id
                KeyType: HASH
              - AttributeName: nombre
                KeyType: RANGE
            Projection:
              ProjectionType: ALL
            ProvisionedThroughput:
              ReadCapacityUnits: 5
              WriteCapacityUnits: 5
        ProvisionedThroughput:
          ReadCapacityUnits: 5
          WriteCapacityUnits: 5
        PointInTimeRecoverySpecification:
          PointInTimeRecoveryEnabled: true

